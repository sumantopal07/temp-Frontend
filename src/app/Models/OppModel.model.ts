export default interface Opp {
  opp_id: number;
  client: string;
  date: string;
  demand: number;
  description: string;
  location: string;
  min_exp: number;
  skill: string;
  user_id: number;
}
